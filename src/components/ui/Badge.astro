---
interface Props {
  class?: string;
}

const { class: className = "" } = Astro.props;
---

<span
  class={`inline-flex items-center rounded-[var(--radius-sm)] border border-border bg-surface-2 px-2 py-0.5 font-mono text-xs font-medium text-text ${className}`}
>
  <slot />
</span>
